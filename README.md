# CustomerioApp SDK

Customer.io App API client, generated from the OpenAPI spec.

Our App API provides ways to trigger messages and retrieve information about people, automations, broadcasts, and more.

Learn more about Customer.io App API at [customer.io](https://customer.io).

This is an unofficial SDK for the Customer.io App public API, generated by Voxgig with [`@voxgig/sdkgen`](https://github.com/voxgig/sdkgen). It is not affiliated with, endorsed by, or sponsored by the upstream API provider.

Learn more about Voxgig SDKs at [voxgig.com/sdk](https://voxgig.com/sdk/).

Full documentation for this SDK: [https://voxgig-sdk.github.io/customerio-app-sdk/](https://voxgig-sdk.github.io/customerio-app-sdk/)

> TypeScript, Python, PHP, Golang, Lua, JavaScript SDKs, a CLI with an interactive REPL, and an MCP server for AI agents — all generated from one OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).

> **Features:** `debug`, `idempotency`, `metrics`, `paging`, `ratelimit`, `retry`, `test`, `timeout` — opt-in,
> inactive until switched on, and configured per client. See the Features
> section of any SDK README below for what each one does.

## Entities, not endpoints

This SDK exposes the API as **53 semantic entities** that you
call directly, instead of assembling URL paths and query strings. See the [Entities](#entities) table below for the full list. Entities are
**Capitalised** to mark them as the primary surface, each with the operations they
support (`list`, `load`, `create`, `update`, `remove`):

```ts
const client = new CustomerioAppSDK()
```

Thinking in entities keeps the mental model small — for people and AI agents alike —
rather than reasoning about raw HTTP routes and query parameters.

## Offline unit testing

Every SDK ships a built-in **test mode** that swaps the HTTP transport for
an in-memory mock, so your unit tests run fully offline — no server, no
network, and no credentials:

### TypeScript

```ts
// The offline mock starts EMPTY — seed it with the records the test needs.
// Shape: { entity: { <entity-name>: { <id>: <record> } } }
const client = CustomerioAppSDK.test({
  entity: {
    collection: {
      test01: { id: 'test01' },
    },
  },
})
const collections = await client.Collection().list()
// collections is an array of Collection entities, populated with mock data
// — call collections[0].data() for the record itself
console.log(collections)
```

### Python

```python
client = CustomerioAppSDK.test()
collections = client.Collection().list()
print(collections)
```

### PHP

```php
// Seed fixture data so offline calls resolve without a live server.
$client = CustomerioAppSDK::test([
    "entity" => ["collection" => ["test01" => ["id" => "test01"]]],
]);
$collections = $client->Collection()->list();
```

### Golang

```go
client := sdk.Test()
result, err := client.Collection(nil).List(
    nil, nil,
)
```

### Lua

```lua
local client = sdk.test()
local results, err = client:Collection():list()
```

### JavaScript

```js
const client = CustomerioAppSDK.test()
const collections = await client.Collection().list()
// collections is an array of entities, populated with mock data
// — call collections[0].data() for the record itself
console.log(collections)
```

## Packages

| Language | Package | Install |
| --- | --- | --- |
| TypeScript | `@voxgig-sdk/customerio-app` | publish pending — [install from git tag](https://github.com/voxgig-sdk/customerio-app-sdk/tags) |
| Python | `voxgig-sdk-customerio-app` | publish pending — [install from git tag](https://github.com/voxgig-sdk/customerio-app-sdk/tags) |
| PHP | `voxgig-sdk/customerio-app` | publish pending — [install from git tag](https://github.com/voxgig-sdk/customerio-app-sdk/tags) |
| Golang | `github.com/voxgig-sdk/customerio-app-sdk/go` | `go get github.com/voxgig-sdk/customerio-app-sdk/go@latest` |
| Lua | `voxgig-sdk-customerio-app` | publish pending — [install from git tag](https://github.com/voxgig-sdk/customerio-app-sdk/tags) |
| JavaScript | `@voxgig-sdk/customerio-app-js` | publish pending — [install from git tag](https://github.com/voxgig-sdk/customerio-app-sdk/tags) |
| Go CLI | `github.com/voxgig-sdk/customerio-app-sdk/go-cli` | `go install github.com/voxgig-sdk/customerio-app-sdk/go-cli/cmd/customerio-app@latest` |
| Go MCP server | `github.com/voxgig-sdk/customerio-app-sdk/go-mcp` | `go get github.com/voxgig-sdk/customerio-app-sdk/go-mcp@latest` |

## Quickstart

### TypeScript

```ts
import { CustomerioAppSDK } from '@voxgig-sdk/customerio-app'

const client = new CustomerioAppSDK({
  apikey: process.env.CUSTOMERIO_APP_APIKEY,
})


// Load a specific automation (returns a Automation)
const automation = await client.Automation().load({
  campaign_id: 1,
})
console.log(automation)
```

See the [TypeScript README](ts/README.md) for the full guide.

## Surfaces

| Surface | Path |
| --- | --- |
| **SDK** (TypeScript, Python, PHP, Golang, Lua, JavaScript) | `ts/` `py/` `php/` `go/` `lua/` `js/` |
| **CLI** | `go-cli/` |
| **MCP server** | `go-mcp/` |

## Use it from an AI agent (MCP)

The generated MCP server exposes every operation in this SDK as an
[MCP](https://modelcontextprotocol.io) tool that Claude, Cursor or Cline
can call directly. Build and register it:

```bash
cd go-mcp && go build -o customerio-app-mcp .
```

Then add it to your agent's MCP config (Claude Desktop, Cursor, etc.):

```json
{
  "mcpServers": {
    "customerio-app": {
      "command": "/abs/path/to/customerio-app-mcp"
    }
  }
}
```

## Entities

The API exposes 53 entities:

| Entity | Description | API path |
| --- | --- | --- |
| **Action** | The Action entity. | `` |
| **Activity** | The Activity entity (list). | `/v1/activities` |
| **Asset** | The Asset entity (create, list, load, remove, update). | `/v1/assets` |
| **Attribute** | The Attribute entity. | `` |
| **Automation** | The Automation entity (list, load, update). | `/v1/campaigns/{campaign_id}/messages` |
| **Broadcast** | The Broadcast entity (list, load, update). | `/v1/broadcasts/{broadcast_id}/actions/{action_id}/metrics/links` |
| **Campaign** | The Campaign entity. | `` |
| **Collection** | The Collection entity (create, list, load, remove, update). | `/v1/collections` |
| **Content** | The Content entity. | `` |
| **Customer** | The Customer entity (create, list, load). | `/v1/customers` |
| **DataIndex** | The DataIndex entity (create). | `/v1/data_index/attributes` |
| **Delivery** | The Delivery entity. | `` |
| **DesignStudio** | The DesignStudio entity (create, list, load, remove, update). | `/v1/design_studio/components` |
| **DesignStudioEmail** | The DesignStudioEmail entity (create, list, load, remove, update). | `/v1/design_studio/emails` |
| **Email** | The Email entity. | `` |
| **End** | The End entity. | `` |
| **EspSuppression** | The EspSuppression entity (create, load, remove). | `/v1/esp/domains/{domain_name}/suppression/{suppression_type}` |
| **Export** | The Export entity (create, list, load). | `/v1/exports` |
| **Import** | The Import entity (create, load). | `/v1/imports/{import_id}` |
| **InApp** | The InApp entity. | `` |
| **InboxMessage** | The InboxMessage entity. | `` |
| **Info** | The Info entity (list). | `/v1/info/ip_addresses` |
| **IpAddress** | The IpAddress entity. | `` |
| **Language** | The Language entity. | `` |
| **Link** | The Link entity. | `` |
| **LiveNotification** | The LiveNotification entity (create, load). | `/v1/live_notifications/{instance_id}` |
| **Message** | The Message entity (list, load). | `/v1/messages` |
| **Newsletter** | The Newsletter entity (create, list, load, remove). | `/v1/newsletters` |
| **NewsletterMetric** | The NewsletterMetric entity (list, load). | `/v1/newsletters/{newsletter_id}/contents/{content_id}/metrics/links` |
| **NewsletterVariant** | The NewsletterVariant entity (create, list, load, remove, update). | `/v1/newsletters/{newsletter_id}/contents` |
| **Object** | The Object entity (create, list, load). | `/v1/objects/{object_type_id}/{object_id}/relationships` |
| **ObjectType** | The ObjectType entity. | `` |
| **OptOut** | The OptOut entity (list, update). | `/v1/optouts` |
| **Push** | The Push entity. | `` |
| **Relationship** | The Relationship entity. | `` |
| **ReportingWebhook** | The ReportingWebhook entity (create, list, load, remove, update). | `/v1/reporting_webhooks` |
| **SearchSuppression** | The SearchSuppression entity. | `` |
| **Segment** | The Segment entity (create, list, load, remove). | `/v1/segments` |
| **SendMessage** | The SendMessage entity (create). | `/v1/campaigns/{broadcast_id}/triggers` |
| **SenderIdentity** | The SenderIdentity entity (list, load). | `/v1/sender_identities` |
| **Sms** | The Sms entity. | `` |
| **Snippet** | The Snippet entity (create, list, remove, update). | `/v1/snippets` |
| **Start** | The Start entity. | `` |
| **SubscriptionCenter** | The SubscriptionCenter entity (list, load). | `/v1/subscription_channels` |
| **SubscriptionChannel** | The SubscriptionChannel entity. | `` |
| **SubscriptionTopic** | The SubscriptionTopic entity. | `` |
| **Suppression** | The Suppression entity. | `` |
| **TestGroup** | The TestGroup entity. | `` |
| **Transactional** | The Transactional entity (list, load, update). | `/v1/transactional` |
| **Trigger** | The Trigger entity. | `` |
| **Update** | The Update entity. | `` |
| **Whatsapp** | The Whatsapp entity. | `` |
| **Workspace** | The Workspace entity (list). | `/v1/workspaces` |

The operations available across these entities are **load**, **list**, **create**, **update**, **remove** — see each entity's
own list above for exactly which it supports.

## Quickstart in other languages

### Python

```python
import os
from customerioapp_sdk import CustomerioAppSDK

client = CustomerioAppSDK({
    "apikey": os.environ.get("CUSTOMERIO_APP_APIKEY"),
})

```

### PHP

```php
<?php
require_once 'customerioapp_sdk.php';

$client = new CustomerioAppSDK([
    "apikey" => getenv("CUSTOMERIO_APP_APIKEY"),
]);

```

### Golang

```go
import sdk "github.com/voxgig-sdk/customerio-app-sdk/go"

client := sdk.NewCustomerioAppSDK(map[string]any{
    "apikey": os.Getenv("CUSTOMERIO_APP_APIKEY"),
})


// Load a specific automation
automation, err := client.Automation(nil).Load(
    map[string]any{"campaign_id": 1}, nil,
)
if err != nil {
    panic(err)
}
fmt.Println(automation)
```

### Lua

```lua
local sdk = require("customerio-app_sdk")

local client = sdk.new({
  apikey = os.getenv("CUSTOMERIO_APP_APIKEY"),
})

```

### JavaScript

```js
const { CustomerioAppSDK } = require('@voxgig-sdk/customerio-app-js')

const client = new CustomerioAppSDK({
  apikey: process.env.CUSTOMERIO_APP_APIKEY,
})


// Load a specific automation (returns the entity)
const automation = await client.Automation().load({
  campaign_id: 1,
})
console.log(automation)
```

## Direct and prepare

For endpoints the entity model doesn't cover, use the low-level methods:

- **`direct(fetchargs)`** — build and send an HTTP request in one step.
- **`prepare(fetchargs)`** — build the request without sending it.

Both accept a map with `path`, `method`, `params`, `query`,
`headers`, and `body`. See the [How-to guides](#how-to-guides) below.

## How-to guides

### Make a direct API call

When the entity interface does not cover an endpoint, use `direct`:

**TypeScript:**
```ts
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

**Python:**
```python
result = client.direct({
    "path": "/api/resource/{id}",
    "method": "GET",
    "params": {"id": "example"},
})
```

**PHP:**
```php
$result = $client->direct([
    "path" => "/api/resource/{id}",
    "method" => "GET",
    "params" => ["id" => "example"],
]);
```

**Go:**
```go
result, err := client.Direct(map[string]any{
    "path":   "/api/resource/{id}",
    "method": "GET",
    "params": map[string]any{"id": "example"},
})
if err != nil {
    panic(err)
}
fmt.Println(result)
```

**Lua:**
```lua
local result, err = client:direct({
  path = "/api/resource/{id}",
  method = "GET",
  params = { id = "example" },
})
```

**JavaScript:**
```js
const result = await client.direct({
  path: '/api/resource/{id}',
  method: 'GET',
  params: { id: 'example' },
})
if (result instanceof Error) {
  throw result
}
console.log(result.data)
```

## Advanced

> Everyday use only needs the sections above. This explains the internals
> behind every call — relevant when writing custom features.

Every SDK call runs the same five-stage pipeline:

1. **Point** — resolve the API endpoint from the operation definition.
2. **Spec** — build the HTTP specification (URL, method, headers, body).
3. **Request** — send the HTTP request.
4. **Response** — receive and parse the response.
5. **Result** — extract the result data for the caller.

A feature hook fires at each stage (e.g. `PrePoint`, `PreSpec`,
`PreRequest`), so features can inspect or modify the pipeline without
forking the SDK.

### Features

| Feature | Purpose |
| --- | --- |
| **DebugFeature** | Request/response capture ring buffer for debugging |
| **IdempotencyFeature** | Idempotency keys for safe retries of mutating operations |
| **MetricsFeature** | Statistics capture: per-operation counters and latency |
| **PagingFeature** | Pagination signals for list operations |
| **RatelimitFeature** | Client-side rate limiting via a token bucket |
| **RetryFeature** | Automatic retry of transient failures with exponential backoff |
| **TestFeature** | In-memory mock transport for testing without a live server |
| **TimeoutFeature** | Per-request timeout with transport abort |

Pass custom features via the `extend` option at construction time.

## Customizing this SDK

This repository contains its own generator (`.sdk/`), so the SDK is
customizable without forking any upstream tool:

- **The model** (`.sdk/model/`) declares everything this project owns:
  package names, versions, active features, per-target settings. It is
  written in [aontu](https://aontu.dev), a JSON-based
  specification language designed for building ontologies: easy to edit
  by hand, and files unify rather than override, so small declarations
  compose into one model. Regeneration re-reads it every time.
- **Templates** (`.sdk/tm/`) and **components** (`.sdk/src/cmp/`) are
  the two layers of generation, copied into this repo: templates are the
  literal per-language source, components generate the API-shaped parts.
- **Regeneration merges.** By default, newly generated content is
  three-way merged into existing files, so generator updates and local
  edits usually converge without manual conflict handling. A project can
  opt for plain overwrite instead.
- **Custom features and entire custom targets** arrive through sdkgen
  packages (`voxgig-sdkgen package add`), on the same rails as the
  bundled languages, and `voxgig-sdkgen doctor` reports any drift from
  what a resync would write.

How-to: [customize and propagate templates](https://github.com/voxgig/sdkgen/blob/main/docs/how-to/customize-and-propagate-templates.md).
The full story: [voxgig.com/sdk/custom](https://voxgig.com/sdk/custom).

## Per-language documentation

- [TypeScript](ts/README.md)
- [Python](py/README.md)
- [PHP](php/README.md)
- [Golang](go/README.md)
- [Lua](lua/README.md)
- [JavaScript](js/README.md)

## Upstream API

This SDK is generated from the upstream OpenAPI specification. It is an
unofficial client and is not affiliated with the API provider.

The OpenAPI spec(s) this SDK was generated from are kept in the
[`.sdk/def/`](.sdk/def/) folder.

- Upstream API: [https://api.customer.io](https://api.customer.io)

## Security

Please report security issues to security@voxgig.com. See [SECURITY.md](SECURITY.md).
Do not open public issues for suspected vulnerabilities.

---

Generated from the Customer.io App API OpenAPI spec by [@voxgig/sdkgen](https://github.com/voxgig/sdkgen).
