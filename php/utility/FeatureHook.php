<?php
declare(strict_types=1);

// CustomerioApp SDK utility: feature_hook

class CustomerioAppFeatureHook
{
    public static function call(CustomerioAppContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
